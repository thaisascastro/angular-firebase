import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ap14-firebase',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
